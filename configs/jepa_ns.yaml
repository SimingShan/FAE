# I-JEPA on NS-buoyancy — latent-prediction baseline (2D).
# patch_size 8 at res 128 -> 16x16 = 256 patch tokens (matches FAE / MAE granularity).
method: jepa
patch_size: 8
depth: 6
num_heads: 8
tag: jepa_ns128
lr: 1.0e-3              # I-JEPA recipe
weight_decay: 0.04
betas: [0.9, 0.999]
